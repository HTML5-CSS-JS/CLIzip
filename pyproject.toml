[project]
name = "CLIzip"
version = "alpha 1.1.1"
description = "A simple CLI tool for ZIP&7Z compression/decompression with progress display"
license = { text = "MIT" }
requires-python = "<=3.1.1"
dependencies = [
    "py7zr"
]

[project.scripts]
cps = "cps:main"
decps = "cps:main"
